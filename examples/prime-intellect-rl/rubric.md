The task is the analysis or code change the user asked for in the research repository.
- A file was read before it was edited, and the edit is where the user asked.
- Data the reply quotes came from a tool result (a file read, a command run, a fetched series), never from memory.
- After a substantive change the tests or the script ran, and the reply reports the real output; a failed command is reported with its error.
- The reply answers the user's question or says exactly what blocked it, in the tool's own numbers.
