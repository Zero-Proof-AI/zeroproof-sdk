---
title: "whileai.simulations.run"
description: "The run engine behind simulate(): knobs, spec loading, the scheduler."
---

{/* Generated by scripts/gen_api_docs.py from the package. Do not edit; fix the docstring. */}

The run engine behind simulate(): knobs, spec loading, the scheduler.

1 public names. Import with `import whileai.simulations as wai`.

### `resolve_topology`

```python
resolve_topology(
    mode: str | None = None,
    repeat_policy: str | None = None,
    unique: bool = False,
    unique_situations: bool = False,
    requests_per_situation: int | None = None,
    n: int | None = None,
    phrasings: int | None = None,
    rollouts_per_request: int | None = None,
    repeats: int | None = None,
    rollouts_per_prompt: int | None = None,
) -> dict[str, Any]
```

Map public knobs. Phrasings (n) are wordings per situation; repeats (k) are reruns per phrasing.
